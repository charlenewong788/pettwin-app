(()=>{const $=s=>document.querySelector(s);let ready=false;function ensurePet(){let pet=$("#pet-sprite");if(pet)return pet;pet=document.createElement("div");pet.id="pet-sprite";pet.setAttribute("aria-hidden","true");pet.innerHTML='<i class="ear left"></i><i class="ear right"></i><i class="tail"></i><span class="body"></span><span class="head"></span><span class="muzzle"></span><b class="eye left"></b><b class="eye right"></b><em class="nose"></em>';document.body.prepend(pet);return pet}function movePet(x,y){const pet=ensurePet();pet.classList.remove("hidden");pet.classList.add("walking");pet.style.left=Math.max(90,Math.min(innerWidth-90,x))+"px";pet.style.top=Math.max(110,Math.min(innerHeight-90,y))+"px";clearTimeout(movePet.timer);movePet.timer=setTimeout(()=>pet.classList.remove("walking"),1800)}function setCoat(hex,light=true){const pet=ensurePet();pet.style.setProperty("--pet-coat",hex);pet.style.setProperty("--pet-cream",light?"#f5f1e8":hex)}function readPhotoColour(file){return new Promise(resolve=>{const img=new Image(),url=URL.createObjectURL(file);img.onload=()=>{const c=document.createElement("canvas"),s=64;c.width=c.height=s;const x=c.getContext("2d",{willReadFrequently:true});x.drawImage(img,0,0,s,s);const d=x.getImageData(0,0,s,s).data;let r=0,g=0,b=0,n=0,light=0;for(let i=0;i<d.length;i+=16){const l=(d[i]+d[i+1]+d[i+2])/3;if(l>36&&l<230){r+=d[i];g+=d[i+1];b+=d[i+2];n++}if(l>185)light++}URL.revokeObjectURL(url);if(!n)return resolve(null);resolve({hex:"#"+[r/n,g/n,b/n].map(v=>Math.round(Math.max(42,Math.min(178,v*.82))).toString(16).padStart(2,"0")).join(""),light:light>n*.12})};img.onerror=()=>resolve(null);img.src=url})}function polishResult(){const line=$("#studio-result");if(line)line.textContent=document.documentElement.lang.startsWith("zh")?"桌面宠物预览已生成。它已经出现在页面上，会根据你的点击慢慢移动。":"Desktop pet preview created. It is now on the page and will gently follow your clicks."}function bind(){if(ready)return;ready=true;ensurePet();movePet(innerWidth*.72,innerHeight*.54);document.addEventListener("pointerdown",e=>{if(e.target.closest("button,input,label,a"))return;movePet(e.clientX,e.clientY)});const input=$("#pet-photo");if(input)input.addEventListener("change",async e=>{const file=e.target.files&&e.target.files[0];if(!file)return;const colour=await readPhotoColour(file);if(colour)setCoat(colour.hex,colour.light);movePet(innerWidth*.7,innerHeight*.52)});const btn=$("#generate-twin");if(btn)btn.addEventListener("click",()=>setTimeout(()=>{polishResult();movePet(innerWidth*.7,innerHeight*.52)},1800));const toggle=$("#pet-toggle");if(toggle)toggle.addEventListener("click",()=>setTimeout(()=>ensurePet().classList.toggle("hidden",toggle.textContent.includes("显示")||toggle.textContent.includes("Show")),0))}document.readyState==="loading"?document.addEventListener("DOMContentLoaded",bind):bind()})();
